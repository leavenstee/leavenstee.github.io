import React, { Component } from 'react';
import '../App.css';

class About extends Component {
    render() {
      return (
        <div id="about">
            <p>Software engineer with focus in iOS but will create with what ever tool makes the most sense.</p>
        </div>
      );
    }
  }

export default About;
